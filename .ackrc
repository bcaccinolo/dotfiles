# Always sort the files
# --sort-files

# Always color, even if piping to a another program
# --color
# Colors
--color-filename=bold red
--color-lineno=bold magenta

# Use "less -r" as my pager
# --pager=less -r

--type-add=ruby=.haml,.rake,.ru
--type-add=css=.scss

# ignore log files
--type-set=log:ext:log
--nolog

# ignore python .virtualenv
--ignore-dir=.virtualenv
--ignore-dir=tmp

--ignore-dir=node_modules
--ignore-dir=venv # < VEO / Zola virtual env
--ignore-dir=static-collected/CACHE

--ignore-directory=is:venv
--ignore-directory=is:node_modules

# ignore tags file
--ignore-file=is:tags
